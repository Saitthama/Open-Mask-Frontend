name: Android Release

on:
  push:
    # branches: [ "main" ]
    tags:
      - 'v*' # Triggert bei jedem Push mit einem Versionstag wie 1.0.0

jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      contents: write # Erlaubt das Erstellen von Releases und Hochladen von Assets
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-java@v3
        with:
          distribution: 'zulu'
          java-version: '21'
          cache: 'gradle'
      - uses: subosito/flutter-action@v2
        with:
          flutter-version: '3.38.1'
          channel: 'stable'
          cache: true
      - name: Get dependencies
        run: flutter pub get
      - name: Run Tests
        run: flutter test

      - name: Decode Android Keystore
        run: |
          echo "${{ secrets.KEYSTORE_BASE64 }}" | base64 --decode > android/app/keystore.jks
      - name: Create key.properties
        run: |
          echo "storePassword=${{ secrets.KEYSTORE_PASSWORD }}" > android/key.properties
          echo "keyPassword=${{ secrets.KEY_PASSWORD }}" >> android/key.properties
          echo "keyAlias=${{ secrets.KEY_ALIAS }}" >> android/key.properties
          echo "storeFile=keystore.jks" >> android/key.properties

      - name: Extract Version
        id: get_version
        run: |
          # Entfernt das 'v' vom Tag (v1.0.0 -> 1.0.0)
          VERSION_NAME=${GITHUB_REF_NAME#v}
          echo "VERSION_NAME=$VERSION_NAME" >> $GITHUB_ENV
          # Nutzt die GitHub Run Number als Build-Nummer
          echo "BUILD_NUMBER=${{ github.run_number }}" >> $GITHUB_ENV

      #- name: Build AAB
      #  run: flutter build appbundle --release --build-name=$VERSION_NAME --build-number=$BUILD_NUMBER
      - name: Build APK
        run: flutter build apk --release --build-name=$VERSION_NAME --build-number=$BUILD_NUMBER
      - name: Build Split APKs
        run: flutter build apk --release --build-name=$VERSION_NAME --build-number=$BUILD_NUMBER --split-per-abi

      - name: Rename APK
        run: mv build/app/outputs/flutter-apk/app-release.apk build/app/outputs/flutter-apk/open-mask-$VERSION_NAME.apk
      - name: Rename armeabi-v7a-APK
        run: mv build/app/outputs/flutter-apk/app-armeabi-v7a-release.apk build/app/outputs/flutter-apk/open-mask-armeabi-v7a-$VERSION_NAME.apk
      - name: Rename arm64-v8a-APK
        run: mv build/app/outputs/flutter-apk/app-arm64-v8a-release.apk build/app/outputs/flutter-apk/open-mask-arm64-v8a-$VERSION_NAME.apk
      - name: Rename x86_64-APK
        run: mv build/app/outputs/flutter-apk/app-x86_64-release.apk build/app/outputs/flutter-apk/open-mask-x86_64-$VERSION_NAME.apk

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          files: |
            build/app/outputs/flutter-apk/*.apk
          #  build/app/outputs/bundle/release/*.aab
          generate_release_notes: true
          tag_name: ${{ github.ref_name }} # Nutzt den Tag-Namen (z.B. v1.0.0)
          name: Release ${{ github.ref_name }}
          draft: false
          prerelease: false
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }} # Wird automatisch von GitHub bereitgestellt
